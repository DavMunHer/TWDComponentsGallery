export interface DynamicCardInfo {
    daysDiff: number
    downloads: number
    version: string
    sizeInKb: number
}

export interface PartialDynamicCardInfo {
    daysDiff: number
    version: string
    sizeInKb: number
}
