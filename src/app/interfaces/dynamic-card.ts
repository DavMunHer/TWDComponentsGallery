export interface DynamicCard {
    daysDiff: number
    downloads: number
    version: string
    sizeInKb: number
}

export interface PartialDynamicCard {
    daysDiff: number
    version: string
    sizeInKb: number
}
